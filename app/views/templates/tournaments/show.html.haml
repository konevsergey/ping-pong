.panel.panel-default.panel-shadow
  .panel-body
    .command-panel
      .pull-left
        %h3
          {{vm.tournament.name}}
      .pull-right
        .btn-group{ ng_show: 'currentUser.admin' }
          =render_button 'edit', ui_sref: 'editTournament(vm.tournament)'
          =render_button 'destroy', ng_click: 'vm.destroy()'
      .clearfix

=render 'templates/rounds/index'
=render 'templates/teams/index'

%div{ ui_view: 'games' }
